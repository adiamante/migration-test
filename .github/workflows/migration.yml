name: Migration test workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Migration test
        run: docker-compose up --abort-on-container-exit --exit-code-from pgtap

